from setuptools import find_packages, setup

setup(
    name='coyote',
    packages=find_packages(include=[]),
    version='0.1.0',
    description='Build html with function components',
    author='Wolfpup Software',
    install_requires=[],
    test_suite='Coyote Tests',
)
